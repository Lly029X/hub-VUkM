基础 02-joint-bert-training-only  中的数据集，希望你自己写一个提示词能完成任务

  基于 02-joint-bert-training-only 数据集，可以设计一个用于信息解析智能对话系统的提示词，让大模型完成领域分类、意图识别和槽位抽取。提示词的核心是：给定候选的 domain、intent 和 slots 标签集合，要求模型从用户输入中抽取对应的结构化信息，并严格按 JSON 输出。例如可设置 system prompt：让模型扮演“专业信息抽取助手”，输入一条中文自然语言，输出 {"domain": "...", "intent": "...", "slots": {...}}。其中 domain 从数据集领域集合中选取，intent 从意图集合中选取，slots 的 key 从槽位标签集合中选取，value 必须来自原句。这样即可将自然语言转换为机器可执行的语义表示，完成信息解析任务。
