1. BERT文本分类和实体识别有什么关系？分别使用什么loss？
关系：在多任务学习框架中，文本分类（如情感分析、意图识别）和实体识别（命名实体识别，NER）通常共享底层的BERT编码器，但使用不同的输出层。
它们可以联合训练，让模型同时学习句子级别的语义和token级别的细粒度信息。通过共享参数，两个任务可以互相促进：
文本分类任务有助于模型捕捉全局语义，而实体识别任务则增强了对局部实体的感知，从而提升整体表示能力。

使用的loss：
文本分类：一般取BERT输出中[CLS]位置的向量，经过一个全连接层后，使用交叉熵损失（Cross-Entropy Loss）计算预测类别与真实标签之间的差异。

实体识别：通常对每个token的输出（或经过CRF层）进行预测，每个token对应一个标签（如B-PER、I-PER、O等）。
常用的loss也是交叉熵损失，计算每个token预测概率与真实标签的交叉熵，然后取平均（或总和）。如果使用CRF（条件随机场）层，则使用CRF的负对数似然损失，考虑标签之间的转移约束。

2. 多任务训练 loss = seq_loss + token_loss 有什么坏处？如果存在训练不平衡的情况，如何处理？
坏处：直接将两个loss相加的缺点在于：
量纲差异：两个loss的数值范围可能不同，例如seq_loss（文本分类损失）可能较小，而token_loss（实体识别损失）由于对每个token求和可能较大，
导致token_loss主导梯度更新，使得模型偏向于优化实体识别而忽略文本分类。
学习速度不同：不同任务的收敛速度可能不同，直接相加可能导致一个任务过拟合而另一个任务欠拟合。
梯度冲突：两个任务的梯度方向可能不一致，简单相加可能导致相互干扰，影响共享层的表示学习。

处理方法（针对训练不平衡）：
加权求和：引入可调节的超参数 λ，如 loss = α * seq_loss + β * token_loss，通过手动调整或网格搜索找到合适的权重。
动态权重调整：使用不确定性加权（如Multi-Task Learning Using Uncertainty to Weigh Losses），将权重作为可学习参数，根据任务的不确定性自动调整。
梯度归一化：如GradNorm，根据梯度的大小动态调整任务权重，使各任务的梯度幅度相近。
任务特定的学习率：对不同任务的输出层使用不同的学习率，减缓某个任务过快收敛。
两阶段训练：先预训练共享层，再分别微调各个任务，或者交替训练。
